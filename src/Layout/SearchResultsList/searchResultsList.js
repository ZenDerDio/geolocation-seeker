import React from "react";
import styles from './searchResultsList.css';

const SearchResultsList = () => {
  return (
      <section className={styles.searchResult}>Search Results</section>
  );
}

export default SearchResultsList;
